pipeline {
	agent { dockerfile true }
	#options {
    #    timeout(time: 2)
    #}
    stages {
        stage('Test') {
            steps {
                sh 'ls'
                sh 'pwd'
            }
        }
    }
	}